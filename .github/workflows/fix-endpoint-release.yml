name: Fix endpoint release

on:
  push:
    branches:
      - agent/issue-15-connector-ergonomics

permissions:
  contents: write

jobs:
  fix:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/issue-15-connector-ergonomics
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python tools/fix_endpoint_release.py
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "fix: preserve connector binding on endpoint release"
          git push origin HEAD:agent/issue-15-connector-ergonomics

# P055-PID-Agent — 当前任务清单

> 历史切片与专题验收保留在 `docs/issue-*.md` 和其他专题文档；本文件只维护当前工作面。

## 当前质量门槛

- [ ] 运行 `pytest -q`、`ruff check backend` 与 `pid-agent quality-harness`。
- [ ] 运行前端 `npm test`、`npm run build` 和 `npm run test:e2e`。
- [ ] 抽查文档创建、端口连接、分支/汇合、撤销/重做和 revision 冲突。
- [ ] 验证项目 JSON 往返、PDF/DXF 和工程报告导出。
- [ ] 对共享部署运行认证/CORS/Provider SSRF 与秘密泄露回归。

## 数据与恢复

- [x] 本机 `data/*.db*` 文件权限收紧为 `0600`，未改数据库内容。
- [ ] 使用 CLI 创建一致备份，在隔离临时路径做一次恢复演练。
- [ ] 为重要数据库和项目包安排独立异机备份。
- [ ] 在 schema 或 migration 变化前后记录版本和恢复证据。

## 产品路线

- [ ] 完善属性、图层、系统显隐和管线折点编辑。
- [ ] 增强自动布局、避让、跨线表达和大型图纸性能。
- [ ] 支持 Agent 的局部移动、替换、删除和重连闭环。
- [ ] 扩展单位图例、历史图纸知识和企业报表模板。
